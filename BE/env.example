# Application Settings
APP_NAME=Video Retrieval API
APP_VERSION=1.0.0
DEBUG=false

# Server Settings
HOST=0.0.0.0
PORT=8000

# Qdrant Settings
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=video_embeddings
QDRANT_VECTOR_SIZE=512

# CORS Settings
CORS_ORIGINS=["*"]
CORS_METHODS=["*"]
CORS_HEADERS=["*"]

# Database Settings (if needed later)
# DATABASE_URL=postgresql://user:password@localhost/dbname
